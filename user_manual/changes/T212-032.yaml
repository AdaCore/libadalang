type: new-feature
title: Add ``p_is_visible`` property

description: |
    This property can be called on a ``BasicDecl`` to determine if this
    declaration is visible from the point of view of a given node.

    .. ATTENTION::
        Only package-level (public or private) declarations are supported
        for now.

date: 2020-03-25
