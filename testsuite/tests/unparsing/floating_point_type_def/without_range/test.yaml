description: |
    This test checks the formatting of a simple floating point type
    declaration without the range specification. It is expected the
    declaration stays on a single line.

driver: unparser
rule: type_decl
