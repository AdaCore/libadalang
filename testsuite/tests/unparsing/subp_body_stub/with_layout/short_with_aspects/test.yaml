driver: unparser
rule: subp_body_stub
description: |
  Test that a line break is added before the `with` keyword and that it has the
  same indentation as the `function` keyword and the parameters are aligned
  following the tall layout style.
overridings:
  - tall
