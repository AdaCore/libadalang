description: |
    This test checks the formatting of a simple incomplete
    formal type declaration with long type name.
    It is expected the declaration breaks after `type` and
    before `or` keywords, the new line is not indented and
    `type` and `or` keywords be aligned.
 
driver: unparser
rule: formal_type_decl
