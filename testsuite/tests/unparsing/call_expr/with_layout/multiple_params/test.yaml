driver: unparser
rule: expr
description: |
  Test that when `f_suffix` has multiple parameters, a line break is added before it
  and each parameter has its own line with tall layout.
overridings:
  - tall
