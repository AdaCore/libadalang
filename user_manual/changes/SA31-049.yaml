type: new-feature
title: Add an ``is_array_slice`` property

description: |
    Add an ``is_array_slice`` property, which tells whether the given
    ``CallExpr`` represents an array slice or not.

date: 2019-10-31
