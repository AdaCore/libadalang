type: bugfix
title: Fix name resolution on ``AttributeDefClause`` nodes
description: |
    Libadalang would previously crash when resolving names on
    ``AttributeDefClause`` nodes of the form ``for X'Address use Y'Address``
    when the ``System`` package was not visible from the clause.
date: 2020-02-14
