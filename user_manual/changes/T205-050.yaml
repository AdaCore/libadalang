type: bugfix
title: Crash in the ``P_Doc`` property

description: |
    Using the ``P_Doc`` property on files that contain carriage return
    characters trigger assertion failures. This is now fixed, and carriage
    returns that are the last line characters are now discarded from
    ``P_Doc``'s result.

date: 2020-02-06
