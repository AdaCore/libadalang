driver: python
input_sources: []
control:
    - [SKIP, "python == 2", "Python 2 is not supported"]
    - [SKIP, "not for_travis_ci",
       "Both LAL and Langkit install required, only on Travis CI"]
