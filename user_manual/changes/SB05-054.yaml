type: bugfix
title: Find_all_references crash with empty files

description: |
    Find_all_references could crash if one or several source files in
    the project was empty. This is because those are parsed as empty
    lists of compilation units which was unexpected. This is now correctly
    handled.

date: 2019-11-13
