driver: unparser
rule: return_stmt
description: |
  Test the `ReturnStmt` on the line length limit.
  It is expected that it breaks after the `return` keyword and
  before the operator.
overridings:
  - tall
