type: new-feature
title: Add a ``p_is_array_slice`` property

description: |
    Add a ``p_is_array_slice`` property, which tells whether the given
    ``CallExpr`` represents an array slice.

date: 2019-10-31
