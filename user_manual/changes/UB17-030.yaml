type: new-feature
title: Grammar rule to parse while loop specs
description: |
    There is now a dedicated grammar rule to parse while loop specs (``while
    X``): ``Libadalang.Common.While_Loop_Spec_Rule`` in the Ada API,
    ``libadalang.GrammarRule.while_loop_spec_rule`` in the Python API.
date: 2021-11-18
