project P2 is

   for Source_Dirs use ("p2");
   for Main use ("proc2.adb");
   for Object_Dir use "p2/obj";

   package Builder is
      for Switches ("proc2.adb") use ("-gnateDX3=1");
   end Builder;

end P2;
