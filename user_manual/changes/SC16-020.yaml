type: new-feature
title: Add property ``p_get_enum_representation_clause``

description: |
    Add the ``p_get_enum_representation_clause`` property on ``BaseTypeDecl``,
    which will return the ``EnumRepClause`` associated to the given type if
    that type designates an enum declaration and such a representation clause
    exists.

date: 2019-12-16

