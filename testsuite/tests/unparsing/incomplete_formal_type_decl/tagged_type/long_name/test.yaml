description: |
    This test checks the formatting of a simple incomplete
    tagged formal type declaration with long type name.
    It is expected the declaration breaks after `type` and
    `tagged` keywords, the new line be indented and `type`
    and `or` keywords be aligned.
 
driver: unparser
rule: formal_type_decl
