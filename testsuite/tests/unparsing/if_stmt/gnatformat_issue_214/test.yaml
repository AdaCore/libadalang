driver: unparser
rule: compilation_unit
description: |
  Test case for an `IfStmt`, if the staments ends with comments,
  these will be always aligned with the following keyword
  (i.e., `else`, `elsif` or `end if`).
