project P is

   for Main use ("foo.adb");

   package Naming is
      for Spec ("Pkg1") use "specs.ada" at 1;
      for Body ("Pkg1") use "bodies.ada" at 1;

      for Spec ("Pkg2") use "specs.ada" at 2;
      for Body ("Pkg2") use "bodies.ada" at 2;
   end Naming;

end P;
